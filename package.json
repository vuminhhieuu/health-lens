{
  "name": "health-lens",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r --if-present build",
    "test": "pnpm run test:db && pnpm -r --if-present test",
    "test:db": "node --test scripts/db/*.test.mjs"
  }
}
